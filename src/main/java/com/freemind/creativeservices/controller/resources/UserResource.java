package com.freemind.creativeservices.controller.resources;/*
package com.freemind.creativeservices.controller.resources;


import static org.springframework.hateoas.server.mvc.WebMvcLinkBuilder.*;
import com.freemind.creativeservices.controller.UserController;
import com.freemind.creativeservices.model.User;
import org.springframework.hateoas.Link;
import org.springframework.hateoas.server.mvc.WebMvcLinkBuilder;

public class UserResource {

    private final User person;

    public UserResource(final User person) {
        this.person = person;
        final long id = person.getUserId();
        Link link = linkTo(UserController.class).withRel("all-user");
        */
/*add(linkTo(methodOn(GymMembershipController.class).all(id)).withRel("memberships"));
        add(linkTo(methodOn(PersonController.class).get(id)).withSelfRel());*//*

    }

}

}*/
